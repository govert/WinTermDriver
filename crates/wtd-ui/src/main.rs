//! `wtd-ui` — WinTermDriver UI process.
//!
//! Graphical Windows application. Creates native windows, renders the tab
//! strip and pane splitters, and renders terminal content using VT bytes
//! received from `wtd-host` over the named pipe.
//!
//! UI rendering technology is determined by the evaluation spike in bead
//! wintermdriver-6en.1. See spec §8.2 and §24.

fn main() {
    eprintln!("wtd-ui: not yet implemented");
}
